DATABASE_URL=postgresql://user:password@host:port/railway
JWT_SECRET=change-me-in-production
CORS_ORIGIN=http://localhost:5173,https://badukstone.up.railway.app
MASTER_EMAIL=bsbaduk
MASTER_PASSWORD=230123
PORT=3001

# Firestore migration (one-time) — copy to .env.local
# DATABASE_URL=postgresql://postgres:...@...proxy.rlwy.net:PORT/railway
#   (Railway dashboard -> Postgres -> Connect -> Public Network)
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
# DEFAULT_ADMIN_PASSWORD=changeme123

# From server/ folder run:
#   .\migrate-railway.ps1
# Or manually:
#   npm run db:migrate
#   npm run migrate:firestore
